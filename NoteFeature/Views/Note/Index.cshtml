@model NoteFeature.Models.ViewModels.IndexViewModel

@{
    ViewData["Title"] = "Index";
    NoteFeature.Models.FilterModel.IndexFilterModel filter;

    if (Model.Filter != null)
    {
        filter = Model.Filter;
    }
    else
    {
            filter = new NoteFeature.Models.FilterModel.IndexFilterModel();
    }

    var fromDateStr = filter.FromDate?.ToString("yyyy-MM-dd", System.Globalization.CultureInfo.InvariantCulture);
    var toDateStr = filter.ToDate?.ToString("yyyy-MM-dd", System.Globalization.CultureInfo.InvariantCulture);

}

<div class="d-flex justify-content-between align-items-center mb-4 user-select-none">
    <h1 class="display-5 mb-0">
        <i class="bi bi-journal-text text-primary"></i> My Notes
    </h1>
    <a asp-controller="Note" asp-action="Create" class="btn btn-success btn-lg shadow-sm">
        <i class="bi bi-plus-lg"></i> Add Note
    </a>
</div>

<!-- filter -->
<form method="get" asp-action="Index" class="row g-3 mb-4">
    <div class="col-md-4">
        <label>Search</label>
        <input class="form-control" type="text" name="SearchTitle" value="@filter.SearchTitle" placeholder="Search by title" />
    </div>

    @* <div class="col-md-2 mb-2">
        <label>Sort By</label>
        <select class="form-select" name="SortBy">
            <option value="">-- Select --</option>
            <option value="CreatedAt" selected="@(filter.SortBy == "CreatedAt" ? "selected" : "")">Created Date</option>
            <option value="UpdatedAt" selected="@(filter.SortBy == "UpdatedAt" ? "selected" : "")">Updated Date</option>
        </select>
    </div> *@
    <!-- Remove reason: protect user from confuse-->

    <div class="col-md-2">
        <label>Sort by</label>
        <select class="form-select" name="SortDirection">
            <option value="Desc" selected="@(filter.SortDirection == "Desc")">Newest to oldest</option>
            <option value="Asc" selected="@(filter.SortDirection == "Asc")">Oldest to Newest</option>
        </select>
    </div>

    <div class="col-md-2">
        <label>From</label>
        <input class="form-control" type="date" name="FromDate" value="@fromDateStr" />
    </div>

    <div class="col-md-2">
        <label>To</label>   
        <input class="form-control" type="date" name="ToDate" value="@toDateStr" />
    </div>

    <div class="col-md-1 d-flex align-items-end">
        <button type="submit" class="btn btn-primary">Search</button>
    </div>
    <hr />
</form>


@if (!Model.Notes.Any())
{
    <div class="alert alert-info text-center user-select-none">
        <i class="bi bi-info-circle"></i> No notes found. Click <strong>Add Note</strong> to create your first note!
    </div>
}
else
{
    <div id="noteListContainer" class="row g-4">
        @* @foreach (var note in Model.Notes)
        {
            <div class="col-12 col-sm-6 col-md-4">
                <div class="card h-100 shadow-sm border-0 note-card">
                    <div class="card-body d-flex flex-column p-4">
                        <h5 class="card-title mb-2 user-select-none">
                            <i class="bi bi-sticky text-warning"></i>
                            @note.Title
                            @if (@note.UpdatedAt != null)
                            {
                                <small class="text-muted">
                                    <span class="badge bg-black">edit</span>
                                </small>
                            }
                        </h5>
                        <p class="card-text flex-grow-1 user-select-none">@note.Content</p>
                        <div class="d-flex justify-content-between align-items-center mt-3">
                            <small class="text-muted user-select-none">
                                <i class="bi bi-calendar3"></i>
                                @note.CreatedAt.ToString("yyyy-MM-dd HH:mm")
                            </small>
                            <div class="d-flex align-items-center">
                                <a asp-controller="Note" asp-action="Detail" asp-route-id="@note.Id" class="btn btn-outline-primary btn-sm me-1" title="Detail">
                                    <i class="bi bi-eye"></i>
                                </a>
                                <div class="d-flex align-items-center">
                                    <a asp-controller="Note" asp-action="Edit" asp-route-id="@note.Id" class="btn btn-outline-primary btn-sm me-1" title="Edit">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <button type="button"
                                            class="btn btn-outline-danger btn-sm"
                                            data-bs-toggle="modal"
                                            data-bs-target="#deleteModal"
                                            data-note-id="@note.Id"
                                            data-note-title="@note.Title"
                                            title="Delete">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        } *@
        <!-- Pagination template from P'TAR project -->
    </div>
        <div class="">
            <div class="row mt-3">
                <div class="col-12 col-sm-6 d-flex justify-content-center justify-content-md-start">
                    <label for="paginationPerPageSelect" class="mr-2 text-gray-700 align-self-center">Rows per page:</label>
                    <select id="paginationPerPageSelect" class="px-3 py-2 border rounded-lg" style="border-radius: 7px; margin-left: 5px;">
                    <option value="6" selected>6</option>
                    <option value="15">15</option>
                    </select>
                </div>
                <div class="col-12 col-sm-6 d-flex mt-4 mt-sm-0 mt-md-0 justify-content-center justify-content-md-end">
                    <nav aria-label="Page navigation">
                        <ul class="pagination pagination-sm d-flex align-items-center mb-0" id="pagination">
                            <!-- ปุ่มจะถูกสร้างโดย JavaScript -->
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
}

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Delete Note</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <span id="deleteModalMessage">Are you sure you want to delete this note?</span>
            </div>
            <div class="modal-footer">
                <form id="deleteForm" method="post">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <style>
        .note-card {
            transition: box-shadow 0.2s, transform 0.2s;
        }
        .note-card:hover {
            box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15);
            transform: translateY(-4px) scale(1.02);
        }
    </style>
    <script>
        var deleteModal = document.getElementById('deleteModal');
        deleteModal.addEventListener('show.bs.modal', function (event) {
            var button = event.relatedTarget;
            var noteId = button.getAttribute('data-note-id');
            var noteTitle = button.getAttribute('data-note-title');
            var form = document.getElementById('deleteForm');
            var message = document.getElementById('deleteModalMessage'); //Change text in modal delete pop up
            
            form.action = '/Note/Delete/' + noteId;
            
            message.textContent = 'Are you sure you want to delete the note "' + noteTitle + '"?';
        });
    </script>

    <!-- Script Pagination from P'TAR Project -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/pagination.js?ver=@DateTime.Now.Ticks.ToString()"></script>
    <script src="~/js/pagination.js?ver=@DateTime.Now.Ticks.ToString()"></script>
    <script src="~/js/note.js?ver=@DateTime.Now.Ticks.ToString()"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            note.init();
        });
    </script>
}